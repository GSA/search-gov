site_domain.domain:
  name: site_domain.domain
  value: Leave off www to include all subdomains.

document_collection.url_prefixes.prefix:
  name: document_collection.url_prefixes.prefix
  value: Enter the folder where the files to be searched are stored. Don't add an index page.

obsolete_key:
  name: obsolete_key
