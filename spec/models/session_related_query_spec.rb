require 'spec_helper'

describe SessionRelatedQuery do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    SessionRelatedQuery.create!(@valid_attributes)
  end
end
