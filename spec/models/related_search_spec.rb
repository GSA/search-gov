require 'spec_helper'

describe RelatedSearch do
  before(:each) do
    @valid_attributes = {
      
    }
  end

  it "should create a new instance given valid attributes" do
    RelatedSearch.create!(@valid_attributes)
  end
end
