version: 2.1
orbs:
  aws-ecr: circleci/aws-ecr@8.2.1
  aws-eks: circleci/aws-eks@1.1.0
  aws-cli: circleci/aws-cli@3.1.4
  kubernetes: circleci/kubernetes@1.3.1
  docker: circleci/docker@1.5.0
jobs:
  update-search-staging:
    docker:
      - image: cimg/aws:2023.01
    steps:
      - checkout
      - setup_remote_docker
      - aws-cli/setup:
          aws-region: STAGING_AWS_REGION
          aws-access-key-id: CIRCLE_CI_USER_ACCESS_KEY_ID # added in circle ci org settings 
          aws-secret-access-key: CIRCLE_CI_USER_SECRET_ACCESS_KEY
      - run: docker build -t searchgov .
      - run: docker tag searchgov:latest 213305845712.dkr.ecr.us-east-2.amazonaws.com/searchgov:latest
      - run: docker tag searchgov:latest 213305845712.dkr.ecr.us-east-2.amazonaws.com/searchgov:${CIRCLE_BUILD_NUM}
      - run: aws ecr get-login-password --region us-east-2 | docker login --username AWS --password-stdin 213305845712.dkr.ecr.us-east-2.amazonaws.com
      - run: docker push 213305845712.dkr.ecr.us-east-2.amazonaws.com/searchgov:latest
      - run: docker push 213305845712.dkr.ecr.us-east-2.amazonaws.com/searchgov:${CIRCLE_BUILD_NUM}
      - kubernetes/install-kubectl
      - run:
          name: update k8s searchgov deployment
          command: |
            aws eks update-kubeconfig --name staging-search-cluster --region us-east-2
            kubectl get pods -n search

   
workflows:
  build-and-update:
    jobs:
      - update-search-staging:
          context:
            - aws-client-keys
          filters:
            branches:
              only:
                # - staging
                # - main
                - container-pipeline
      