.uncrawled-urls
  = affiliate_center_breadcrumbs([link_to(@affiliate.display_name, affiliate_path(@affiliate)), link_to('URLs', urls_affiliate_path(@affiliate)), "Uncrawled URLs"])
  %h1 Uncrawled URLs
  = render_help_link(@help_link)
  .uncrawled-url-list
    %table
      %colgroup
        %col{ :class => 'url' }
      %thead
        %tr
          %th{ :colspan => 2 }
            - unless @uncrawled_urls.blank?
              %span= page_entries_info @uncrawled_urls, :entry_name => 'Uncrawled URL', :html => false
      = render :partial => 'uncrawled_urls', :locals => { :shorten_url_length => 40 }
    = will_paginate @uncrawled_urls