.rss-feeds
  = affiliate_center_breadcrumbs([link_to(@affiliate.display_name, affiliate_path(@affiliate)), "RSS"])
  .column-1
    %h1 RSS
    %p
      Would you like searchers to be able to search your RSS feeds and narrow results by date? Tell us the location of your feeds.
      We will index the feeds so that they appear in your results within minutes.
    .rss-feed-list
      %table
        %colgroup
          %col{ :class => 'url' }
          %col{ :class => 'name' }
          %col{ :class => 'status' }
          %col{ :class => 'actions' }
        %thead
          %tr
            %th{ :colspan => 4 }
              = link_to "Add new RSS feed", new_affiliate_rss_feed_path(@affiliate), :class => 'add-link'
              - unless @rss_feeds.blank?
                %span= page_entries_info @rss_feeds, :html => false
        %tbody
          %tr.column-header
            %th Url
            %th Name
            %th Is Active?
            %th Actions
          - unless @rss_feeds.blank?
            - @rss_feeds.each do |rss_feed|
              %tr.row-item
                %td= link_to rss_feed.url.truncate(35, :omission => '...' ), [@affiliate, rss_feed]
                %td.name= rss_feed.name
                %td.is_active= rss_feed.is_active ? "Active" : "Inactive"
                %td.actions
                  = link_to("Edit", edit_affiliate_rss_feed_path(@affiliate, rss_feed))
                  = button_to("Delete", [@affiliate, rss_feed], :method => :delete, :confirm => "Are you sure you want to delete this RSS feed?")
          - else
            %tr
              %td.empty{ :colspan => 4 } Site #{@affiliate.display_name} has no RSS feeds
      = will_paginate @rss_feeds
  .column-2
    .side-note-image= image_tag "side_notes/rss_1.png"
    .side-note-image= image_tag "side_notes/rss_2.png"
    .side-note
      %h5 Did you know?
      Add one or more specific URLs by using the
      = link_to "URLs", affiliate_superfresh_urls_path(@affiliate)
      option in the left-hand menu.

    .side-note
      %h5 Unsure if your RSS is valid?
      We support RSS 2.0 and Atom feeds. Learn more at:
      %ul
        %li= link_to "RSS 2.0 specification", "http://www.rssboard.org/rss-specification"
        %li= link_to "Atom syndication format", "http://www.atomenabled.org/developers/syndication/"
