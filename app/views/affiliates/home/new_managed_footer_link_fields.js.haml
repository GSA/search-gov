:plain
  jQuery('.footer-links tbody').append("#{escape_javascript(render('managed_footer_link_fields', :link => Hash.new, :index => Time.now.to_i))}");
  setupDragAndDropOnManagedLinks('footer');
  jQuery('.footer-links .title').last().focus();

