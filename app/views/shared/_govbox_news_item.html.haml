.newsitem.searchresult{:id => "newsitem#{index+1}"}
  - module_name = hit.instance.rss_feed.is_video? ? 'VIDS' : 'NEWS'
  = link_with_click_tracking(truncate_html_prose_on_words(highlight_hit(hit, :title), 140).html_safe, hit.instance.link, @affiliate, @search.query, index, module_name, @search_vertical)
  %h4
    %span.feed_name= hit.instance.rss_feed.name
    = time_ago_in_words(hit.instance.published_at)