.group
  .row.center
    %img{:src => "/m/i/logo.gif"}
- form_tag(search_path, :method => :get, :id=>"search_form") do
  = its_beta if @affiliate
  .group
    .row.center
      = text_field :search, :query, {:name=>"query", :placeholder => "Search"}
      - unless (is_device?("iphone") || is_device?("ipod")) then
        = submit_tag(t :search, :id => "search_button")
      = hidden_field_tag :locale, I18n.locale
    .row.center
      = image_tag("binglogo.gif")
- if js_enabled_mobile_device?
  :javascript
    document.getElementById("search_query").focus();
