%h3
  Register to Use APIs and Web Services
%p
  Access to our APIs and web services requires authentication. Log in or register for a new account below.
#login.span-9
  - form_for @user_session, :url => user_session_path, :html => {:class=>"bordered"}  do |f|
    %h1
      Login
    = f.error_messages(:header_message => nil, :class => "error")
    = render :partial => 'user_sessions/form', :object => f
    %p
      = link_to "Forgot your password?", new_password_reset_path
#register.span-9
  - form_for @user, :url => developers_path, :html => {:class=>"bordered"} do |f|
    %h1
      Sign up
    = f.error_messages(:header_message => nil, :class => "error")
    = render :partial => "users/form", :object => f