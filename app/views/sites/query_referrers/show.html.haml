= content_for_site_page_title @site, 'Referrers leading to a Query'
.sub-nav-heading
  %h2 Referrers leading to a Query
  .action-bar
    %ul
      = list_item_with_link_to_current_help_page

%h3= "Top Referring URLs for the query term '#{@query}' from #{@start_date} to #{@end_date}"

%table.table.table-striped.table-bordered.table-kv-pairs
  %thead
    %tr
      %th Top Referring URLs
      %th # of Queries
  %tbody
    - @top_urls.each_with_index do |(url, total), idx|
      %tr
        %td
          = "#{idx+1}. "
          = linked_shortened_url_without_protocol(url)
        %td= link_to number_with_delimiter(total), site_referrer_queries_path(@site,
          {:url => url, :start_date => @start_date, :end_date => @end_date})

%p Tip: Select the hyperlinked number in the # of Queries column to view the top queries searched on for each URL.
