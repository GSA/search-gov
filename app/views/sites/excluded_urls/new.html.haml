= content_for_site_page_title @site, 'Add Filter URL'
= form_for @excluded_url, url: { action: :create }, html: { class: 'form' }  do |f|
  .sub-nav-heading
    %h2 Add Filter URL
    .action-bar
      %ul
        = list_item_with_link_to_current_help_page
        %li= link_to 'View All', site_filter_urls_path(@site)
        %li= f.submit 'Add', class: 'btn submit disabled', disabled: true
  = render_flash_message
  = render_error_messages(@excluded_url)
  = f.label :url, 'URL', class: 'required'
  = f.text_field :url, class: 'required input-primary'
