.l-site-header
  .l-site-header-inner
    %h1
      Admin Center
      %span.beta Beta
    - unless @site.new_record?
      .site-display-name
        %span.label Display Name:
        #{@site.display_name} (#{@site.name})
        = site_pin @site
