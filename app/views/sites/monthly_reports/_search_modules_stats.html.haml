%h3 Impressions and Clicks by Module
%table.table.table-striped.table-bordered.table-analytics#by_module
  %thead
    %tr
      %th Module
      %th Impressions
      %th Clicks
      %th Your CTR
      %th Average CTR
  %tbody
    - search_module_stats.each do |stat|
      %tr
        %td
          = stat.display_name
          - if BestBetType.module_tags.include?(stat.module_tag) and (params[:mmyyyy].nil? or params[:mmyyyy] == Date.current.strftime('%m/%Y'))
            = link_to '(drill down)', site_best_bets_drill_down_path(@site, module_tag: stat.module_tag)
        %td= number_with_delimiter(stat.impressions)
        %td= number_with_delimiter(stat.clicks)
        %td
          = number_to_percentage(stat.clickthru_ratio, :precision => 1)
          = image_tag Gchart.sparkline(:data => stat.historical_ctr, :size => '70x17', :line_colors => '0077CC')
        %td= number_to_percentage(stat.average_clickthru_ratio, :precision => 1)
