- if search.results.present?
  = render 'shared/tweet_gov_box', :search => @search if search.tweets and search.tweets.results.any?
  = render 'shared/jobs_gov_box', :search => @search if search.jobs.present?
