#header-tagline-wrapper
  #header-tagline-inner
    = header_tagline_logo(affiliate)

    - if !affiliate.header_tagline_url.blank?
      #header-tagline
        =sanitize(link_to affiliate.header_tagline, affiliate.header_tagline_url)
    - else
      #header-tagline
        = affiliate.header_tagline