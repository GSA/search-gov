#results
  - @search.results.each_with_index do |instance, index|
    = render partial: 'news_result', locals: { instance: instance, position: search.startrecord + index }
  .content-block-item.content-block-item-footer
    .content-provider= serp_attribution search.module_tag
