The bulk upload job for <%= @results.file_name %> has
finished.

There were <%= @results.total_count %> affiliates.

<% if @results.error_count == 0 %>
There were no errors.
<% else %>
<%= @results.error_count %> affiliates failed validation.
<% end %>

<%= @results.ok_count %> affiliates were updated successfully.

<% @results.affiliates.each do |id| %>
  <%= id %>
    <% @results.affiliates_with(id).each do |error_message| %>
      <%= error_message %>
    <% end %>
  <% end %>

